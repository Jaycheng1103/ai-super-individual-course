# 版本與驗證範圍

v2.0.0，2026-09-22。

## 已完成

- 本 repo 僅提供課程實作流程；完整書稿、PDF、EPUB 及書籍原稿包已從目前版本移出，原本的獨立交付檔保留。
- 學員流程：19 個階段，依錄影順序核對來源，包含逐題訪談、日常工作、內容創作、拍攝剪輯、變現飛輪、服務入口與行動計畫。每階段提供輸入、步驟、產物與驗收條件。
- 斷點：安裝 AIS-OS 前即可保存進度；記錄動作、下一題、成果位置與阻塞。v1 進度保留備份並要求核對，未驗證的舊狀態不直接視為新版完成。
- 來源：依當日助教教材連結核對原有 8 個 GitHub repo，另依講師新增要求加入 Agent Reach，共 9 個來源，固定 commit 與來源包 SHA-256；另附 LLM Wiki、Typeless、Skool、Pinterest 與 API 官方文件。
- v1 既有隔離安裝驗證（Agent Reach 新增前的 8 個來源）：在新的暫存工作區實際初始化 AIS-OS，並分別安裝至 Codex 與 Claude Code 的專案技能位置。每種 agent 6 組套件，共 57 支額外技能，另有 AIS-OS 內附 6 支；附件與授權保留。
- 行銷共用 tools、說人話 references、內容三件組附件、口播可編輯母版均有讀回。兩種 agent 合計 116 筆檔案完整性紀錄（含兩份行銷共用資源），全部相符；相同版本重複安裝不覆寫。
- helper 測試涵蓋：拒絕覆蓋既有資料、路徑跳脫、不跟隨來源符號連結、來源雜湊不符、同名客製技能衝突、進度接續，以及未授權不呼叫 API／不上傳音訊。
- API helper 使用固定官方端點；單元測試以模擬回應驗證資料流，不使用講師的金鑰替學員呼叫付費 API。

## v2 本次驗證

- 20 項單元測試通過，涵蓋開始前進度、逐題斷點、舊版備份與核對、缺失或外部成果路徑拒絕、既有資料保護、未知進度格式停止，以及略過項目不算全部完成。
- 在 macOS 全新暫存資料夾實際執行 start → 保存筆記與進度 → 使用鎖定 AIS-OS 來源包 init → 讀回斷點。前段筆記保留，Codex 與 Claude Code 的六個內附技能各自存在；直接 init 路徑亦驗證通過。
- 19 個階段的輸入、動作、產物、驗收、來源與 Markdown 連結檢查通過。完整書稿資料夾不存在於目前版本。

## 尚不代表

上述安裝測試在 macOS 的隔離資料夾完成，檢查的是檔案安裝、版本、保留原件與讀回；不是每一支技能都已在每個 agent 完成真實創作，也不是 Windows 實機測試。每位學員仍須依階段驗收自己的 App、帳號、MCP、API、轉錄和影片輸出。

來源與 SHA-256 是整理日的快照；上游日後變更不會自動套用。OpenChatCut 桌面 App 與 OpenSlide 執行環境依官方版本另行安裝。這份 repo 不會自動公開發文、批次寄邀請、收費或代購方案。

原始課程機器轉錄尚未逐句回聽，實作流程的整理界線見 docs/course-sequence.md。不包含私人飛書對話與原始影音。公開版使用全新 Git 歷史，不攜帶含書稿的舊提交；GitHub Pages 未啟用。

## 可重跑檢查

```sh
python3 -m unittest discover -s tests -v
python3 scripts/validate_repo.py
python3 scripts/course.py doctor
```

完整安裝測試另需網路下載 sources.lock.json 固定的來源包，並使用全新、可移除的測試工作區；不能拿講師或學員的正式第二大腦當測試資料。

## 課堂已準備專案的入口驗證

預設課前已建立 Codex 專案並手動連接 Google 外掛，AI 直接安全安裝 AIS-OS，再開始訪談。外掛準備狀態與功能實測分開；雲端讀取留到首次使用時驗證。

新增 init --in-place：寫入前檢查所有同名衝突，允許教材 repo 位於私人專案的獨立子目錄。已在 macOS 全新模擬學員專案，以鎖定 AIS-OS 來源實際安裝、保留原筆記，再安裝內容三件組並驗證完整性；回填開場紀錄後，status 的下一步為 background。20 項測試包含整批衝突攔截及仍禁止以教材 repo 本身為私人工作區。未使用學員帳號或呼叫付費 API，未做 Windows 實機驗收。

## Agent Reach 新增驗證

2026-09-22：加入第 7 階段，來源固定為 `a19a171fa980a0785849596492e0af4db800c82f`，下載包 SHA-256 已驗證。課程整合版 Skill 保留 Tavily 優先與本機授權規則；上游原始 Skill、7 份 references 與 MIT 授權均保留。

- 21 項單元測試、19 階段結構及 Markdown 連結檢查通過。
- 使用真實鎖定來源包，在新的 macOS 暫存學員工作區分別安裝 Codex、Claude Code Skill；重複安裝通過，同名客製修改會攔截且保留。完整性讀回相符，上游原件未改動。
- 依教材指令建立全新 venv，從已核對的來源副本安裝 Agent Reach v1.5.0，`pip check` 通過；直接執行教材 RSS 程式取得 3 筆非空標題與 URL。
- CLI 診斷另在此測試 venv 執行；它仍會看到本機既有工具與設定，故不將診斷的平台可用數當作全新學員的結果。未驗收 Windows、登入社群或影片字幕；這些依學員需要另測。

helper 只安裝 Skill 檔案；AI 按同階段指引接續安裝隔離 CLI 與實測。來源程式碼固定，但 Python 相依套件版本仍由安裝當下解析，不能視為所有電腦完全相同的執行環境。

## 2026-09-23 公開版

公開版只從已檢查的現行教材檔案建立，不複製舊 Git 歷史。書稿、PDF、EPUB、下載包與私人報名資料不在本版；舊庫另外保留為私人備份。入口與排錯已改成不需邀請或登入的公開下載流程。原創內容採 MIT，第三方授權維持原樣。
